# [세션] 2026-08-02 스튜디오 상단 메뉴바(Codex식) 신설

- 날짜: 2026-08-02
- 프로젝트/주제: 아호비 웹 스튜디오에 Codex식 상단 메뉴바(파일·편집·보기·도움말) 설계+구현 (M1+M2)

## 결정 사항
- ← →(뒤로/앞으로) 히스토리는 M3로 미룸 (보스 승인). 지금은 disabled 자리만 예약. M3 구현 시 nav.js에 화면 상태 스택 신설 예정.
- 신규 기능은 단축키 모달 + 대화 내보내기(.md)만. 대화에서 찾기·인쇄는 보류(메뉴에도 안 넣음).
- 사용 설명서는 앱 내 모달(manual.html iframe 재사용). 라벨은 표준 그대로 파일·편집·보기·도움말.
- 화면 캡처·업데이트 확인·문제 보고도 M3 (위임 통로/가드 명령 없음, 셸 수정 0 원칙과 충돌).

## 진행 상황 (완료)
- docs/ARCHITECTURE_MENUBAR.md 설계 문서 (§9 구현 기록 포함).
- web/static/menubar.js 신설: 메뉴 테이블 전 항목이 기존 버튼 위임(click()), ✓ 상태는 열 때 DOM에서 읽음(캐시 없음), 형제 호버 즉시 전환, 키보드(↑↓←→/Esc), 전역 Ctrl+N(새 대화)·Ctrl+K(통합 검색). 팝업 창(msn/org/room)은 POP_* 가드로 제외.
- app.css: 높이 단일 출처 --menubar-h(body.mb-on에서 36px), #app 높이 calc와 #work top만 참조. 우상단 예약 구역 목록에 #menubar 추가(padding-right 120px). 드롭다운은 .aho-ctx 룩 공유(z 85).
- shell.js: initApp 끝에 AhoMenubar.init() 1줄. 셸(combine/*.py) 수정 0, 새 가드 명령 0.
- 상주창(420px)은 압축형 ☰ 하나로 같은 트리 접음. 로그인 화면·팝업 창엔 메뉴바 없음.
- 단축키 모달 최신화: 단일 출처 client/hotkey.py 기준. 전역 섹션(Alt+Space 코드 모드 2초, 코드 모드 중 Alt+↑ 입력창 토글 / Alt+↓ 상주창·연속↓ 숨김 / Alt+←→ 단계, PC 앱에서만 표시) + 스튜디오 섹션(Ctrl+N/K, Enter, Ctrl+V, @이름 태그, Esc, / 설명서 검색).
- 검증: 브라우저 DOM으로 짤림 감사(UI_GUARDRAILS §5.5) 3개 모드 0건, winCtl 겹침 0, 시큐어 톤(#f0f8f3) 확인. PC 콤바인 앱 실행해 로그인 게이트에서 메뉴바 미표시(설계대로)까지 확인. 로그인 뒤 육안 확인은 보스 몫(클로드 키 입력 필요).
- 커밋: ahovye 171154f "feat(studio): 상단 메뉴바(Codex식) + AI 서버 잡 + 병행 세션 작업 일괄" (origin/main 581bb11 위, 미푸시).

## 다음 세션에서 필요한 맥락
- M3 남은 것: ← → 히스토리(nav.js, 우측 컬럼 배타 전환과 얽혀 회귀 위험 최대), 화면 캡처 위임(plus.js 공개 트리거 필요), 업데이트 확인·문제 보고(가드 명령 신설 필요).
- 진행 맵(#mMap)은 개발현황 보드 '운영 맵' 탭으로 통합돼 버튼 삭제됨. 메뉴바에도 없음.
- 사고 기록: 같은 저장소에 병행 세션 둘이 동시에 git 커밋 정리를 하다 서로의 스테이징/amend를 세 차례 뒤엎음. 최종 정리는 origin/main 기준 rebase + 메시지 보강으로 해결. 교훈은 app-factory PLAYBOOK 5-2에 이미 반영돼 있음(git 작업도 한 저장소 한 세션).
- 커밋 시점 작업 트리에 병행 세션의 모드 스위처 작업(web/index.html, app.css, secure.js)이 미커밋으로 남아 있음(의도적 보존). 루트의 아호비_IR자료.html은 untracked로 남겨둠(커밋 여부 보스 결정 필요).
- NotebookLM: 인증은 살아 있으나 라이브러리에 노트북 0개 — "Claude Memory" 노트북 공유 URL을 add_notebook으로 등록해야 저장 가능.
- 노트북 URL 등록 시도(2026-08-02 저녁): 크롬 확장 미연결, MCP 프로필은 서버가 잠금(크롬 코드 21). 프로필 복사→patchright(채널 chrome) 조회 직전에 보스가 마감 지시, 복사본은 삭제함. 다음 세션: 보스가 notebook.google.com에서 "Claude Memory" 노트북(없으면 생성) 주소만 주면 add_notebook 한 번으로 끝. 자동 조회를 다시 하려면 프로필 복사(robocopy, Cache류 제외) 후 복사본으로 launchPersistentContext(channel chrome, headless는 신형 크롬이라 불가→창 밖 배치) 경로가 준비돼 있음.
- 메모리 파일: ~/.claude/projects/C--/memory/ahovye-menubar.md
