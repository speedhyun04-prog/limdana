# [세션] 2026-08-02 아호비 계정별 맞춤 지침

- **날짜**: 2026-08-02
- **프로젝트/주제**: ahovye 웹 스튜디오, 계정별 아호비 맞춤 지침(개인 시스템 프롬프트) 기능

## 결정 사항
- 맞춤 지침 입력은 채팅 명령이 아니라 **설정 화면(설정 모달 '아호비' 탭)** 방식으로.
- 지침은 users.json 계정 항목(custom_prompt)에 저장, 상한 2,000자, 빈 값 = 삭제.
- 주입은 기본 시스템 프롬프트 **맨 뒤에 append** (앞부분 공통 유지로 프롬프트 캐시 보존)
  + "기본 규칙(정체성·안전·회사 정책) 우선" 경계 문장 포함. 매 호출 새로 읽어 저장 즉시 반영.
- 저장 버튼 없이 **카톡식 자동 저장** (입력 멈춤 0.8초 + 블러 시, 동일값 재전송 안 함).
  모달의 보라색 주 버튼은 '닫기' 하나로 통일 (보스 오더: 버튼 위치 통일성, 카톡 참조).
- 오브(데스크톱)·음성(SYSTEM_PROMPT_VOICE) 쪽 맞춤 지침 적용은 **다음 단계로 보류**.

## 진행 상황 (완료)
- auth.py: get/set_custom_prompt, CUSTOM_PROMPT_MAX=2000.
- web/client_api.py: POST /api/persona/get, /api/persona/set (본인 세션 토큰).
- web/server.py: _system_prompt_for 가 지침 주입 (호칭 치환 뒤, 조회 실패는 조용히 무시).
- web/static/settings.js: '아호비' 탭 (로그인 계정만, 값 로드 전 편집 잠금), 자동 저장.
- 짤림 픽스: app.css 520px 이하 가로 탭줄에 flex-wrap 추가.
  원인: 아호비 탭 추가로 PC앱 전용 '창' 탭까지 6개가 되자 마지막 탭이 모달 밖으로 밀림.
  브라우저 검증에선 IS_PCAPP 탭이 없어 5개라 감사에 안 걸렸던 것 → 6탭을 DOM 주입으로
  재현해 420px 짤림 감사 0건 확인.
- 문서: docs/ARCHITECTURE_ONBOARDING.md §5-2 API 표에 persona 엔드포인트 추가.
- 검증: pycheck·node --check, 격리 스모크(임시 users/state, 포트 8799, 가짜 두뇌),
  starlette TestClient 라우트 테스트, 실브라우저 자동저장·짤림 감사 모두 통과.
- 운영 반영: 8765 서버 재시작(숨김 pythonw web/run_server.pyw, 세션 state.json 복원 확인).
  이후 픽스는 정적 파일뿐이라 재시작 불필요.
- git: 4b4a17f(기능 + 오늘 작업 일괄), 3f93914(짤림+자동저장 픽스) 커밋·푸시 완료.
- PLAYBOOK 5-8 신설: "조건부 UI 요소(플랫폼·권한·상태별)는 검증 때 최대 조합을 재현해서
  감사한다" (이번 PC앱 전용 탭 짤림이 사례).

## 오후 연장 1: 오브 지침 미적용 3단 디버깅 (전부 해결)
- 1차: "충돌 시 기본 우선" 경계 문장이 기본 집사 톤과 맞물려 말투 지침을 통째로
  누름 → 스타일(말투·호칭·형식)은 지침 우선, 정체성·안전·정책만 기본 우선으로
  재설계 (63a7d71).
- 2차: 토큰 없는 로컬 익명 호출(오브 브리지 조회 실패 경로)에 지침 미주입 →
  익명 로컬 = 보스(convos.LEGACY_OWNER) 폴백 (843bfe7).
- 3차(진범): 보스 화면의 오브는 콤바인 오브가 아니라 **구판 jarvis.py HUD** —
  서버 무경유로 brain 직접 호출이라 위 수정 전부 무관했다. 증거: 대화가 서버
  convos 에 없음 + 프로세스 목록. auth.persona_tail 헬퍼 신설(지침 블록 단일
  출처), jarvis 가 매 턴 SYSTEM_PROMPT_VOICE+persona_tail 로 호출 (3cbd3bc).
- 실측: resume 도 새 시스템 프롬프트를 반영한다(격리 2턴 실험, '멍멍' 테스트).
- 보스 확인: "애교 부린다" — 완료.

## 오후 연장 2: 상주창 단축키 즉시 타이핑 (해결)
- 리포트: 단축키로 상주창을 불러도 클릭 없이 타이핑이 안 됨(재발).
- 헛발 2회: ↓(raise-type) 경로만 수정 — 런처 raise-type 화(2971d99),
  런처 ASFW 양도(b8b3d3b). 그런데 런처 로그를 까보니 **보스 실사용 제스처는
  Alt+Space 뒤 ←/→(단계 확장·축소)**였고 이 경로엔 입력창 커서가 없었다.
- 최종: 화살표 확장·축소 모두 shell.raise_and_type(포그라운드+웹뷰+입력창 커서),
  런처 화살표·↑ 에도 AllowSetForegroundWindow 양도 (ba3cd71). 보스 확인 완료.
- 재시작한 프로세스: 웹 서버(8765), jarvis, studio_web(셸), 런처 — 전부 새 코드.

## 남은 작업
- 지침 미적용 표면(계정 개념 없음, 보류): 구판 데스크톱 studio.py 대화 경로,
  시큐어(local_brain), remote_gate(보스 개인 폰 접속).
- (해소) 트레이 에이전트 화살표 ASFW 양도 추가 완료(581bb11). 단, 사원 PC 에
  닿으려면 릴리스 게시가 필요(installer/release.py, 보스 오더 시) — 셸의
  raise_and_type 화살표 변경(ba3cd71)도 같은 릴리스에 함께 나간다.

## 이 세션의 교훈 (PLAYBOOK 반영)
- 5-8 신설(오전): 조건부 UI 요소는 최대 조합으로 재현해 감사.
- 5-9 신설(오후): "안 됨" 리포트는 실행 로그·프로세스·데이터 흔적으로 사용자의
  실제 경로부터 확정하고 고친다 — 대표 경로 추측 수정 금지.
- 메모리 갱신: ahovye-combine-hotkey-gpu.md 에 "4방향 raise_and_type 계약 +
  ASFW 양도 필수 + 보스 실사용 제스처는 ←/→" 추가.

## 다음 세션에서 필요한 맥락
- 계약 단일 출처: docs/ARCHITECTURE_ONBOARDING.md §5-2 (persona API),
  UI_GUARDRAILS §5.5 (짤림 감사), PLAYBOOK 5-8.
- 운영 서버: pythonw C:\Users\USER\dev\ahovye\web\run_server.pyw (0.0.0.0:8765,
  크래시 자동 재기동, 로그 web/server.log). 재시작 = 프로세스 kill 후 같은 명령.
- 격리 테스트 관례: AHOVYE_WEB_USERS·AHOVYE_WEB_STATE 를 스크래치로, 포트 8799,
  AHOVYE_WEB_FAKE_BRAIN=1 (단, _system_prompt_for 는 가짜 모드에서 None).
- 설정 모달 탭은 이제 6개(PC앱): 내 계정·아호비·알림·음성·화면·창. 탭 추가 시 wrap 확인.
