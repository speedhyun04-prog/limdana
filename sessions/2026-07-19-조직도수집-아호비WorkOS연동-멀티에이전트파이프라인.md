# [세션] 2026-07-19 잔디 조직도 수집·아호비/WorkOS 연동·멀티에이전트 파이프라인·오늘 4트랙

- 날짜: 2026-07-19
- 프로젝트/주제: 멀티에이전트 자동개발 파이프라인 구축, 잔디 조직도 수집→아호비/WorkOS 연동, 오늘 A/C/D 완료·B는 병렬세션 이관

## 결정 사항
- 멀티에이전트 자동개발 파이프라인 세팅: auto-dev(설계→모듈병렬구현→검증·수정루프→통합), auto-fix(버그→모듈라우팅→수정→검증). 진입은 /dev, /fix 스킬 또는 자연어("만들어줘/고쳐줘").
- 잔디 조직도는 데스크톱 앱이 아니라 크롬 웹(로그인 후)에서 수집. 인증 토큰 추출은 시스템이 차단→시도 안 함. 대량 PII는 브라우저가 디스크로 직접 저장(모델 컨텍스트 유입 방지).
- BA-WorkOS 자동 업데이트는 "세션 종료 시 자동커밋"(Stop 훅). 원격 없어 push 없음, worklog 자동생성으로 기존 커밋차단 훅 통과.
- 조직도는 단일 원본 JSON 하나를 아호비·WorkOS가 공유(경로 override 가능). PII는 양쪽 repo에서 gitignore.
- 웹 전환(B)은 오늘 별도 세션에서 병렬 진행하기로 함 → 이 세션은 web/ 손대지 않음.

## 진행 상황 (완료)
- 파이프라인: dev\scripts\claude-workflows\{auto-dev.js,auto-fix.js} + ~/.claude/agents/{architect,module-dev,module-fixer}.md + ~/.claude/skills/{dev,fix}. 버그 수정: Workflow가 args를 문자열로 넘겨 실패→상단에 `if(typeof args==='string')args=JSON.parse(args)` 방어코드 추가.
- 조직도 수집: 컨티뉴엄파트너스(주) 66명 전원(이름/이메일/전화/상위부문/부서/직책/직급) + 27부서 트리 → dev\ahovye\data\org\curlyshyll_org_directory.json. (수집법: 크롬 로그인→조직도 패널→AngularJS `.department-item` 스코프 filteredList의 memberCount===66 루트 members 배열→브라우저 다운로드)
- 아호비: directory_mcp.py(조회 MCP 4도구: search/department/contact/org_tree) + connectors.py "사내연락망" 등록. 실제 MCP stdio 스모크 통과, 커넥터 ON 저장. docs/ARCHITECTURE.md. 커밋됨.
- BA-WorkOS: directory_client.py+directory_schema.py(조회, mock폴백)+config DIRECTORY_JSON_PATH, 자동커밋 Stop 훅(.claude/hooks/auto_commit.py), 메일 워크플로 이름→수신자 해석 배선(resolve_recipient_names). 전체 56테스트 통과, quality error 0. 3커밋.
- 오늘 A(조직도 마무리+WorkOS 메일배선) 완료. C(디자인팀 스튜디오 통합: 🎨 토글, StudioWorker dev/design 분기, 개발진과 상호배타, admin전용, 오프스크린 스모크) 완료·커밋. D(파이프라인 실전검증: auto-dev로 tools/orgcli 자율생성, 에이전트12개·708k토큰·생성테스트24개 통과·다중루트 tree 엣지케이스까지 처리) 완료.
- B(웹 전환): 이 세션에서 web/server.py 첫 슬라이스(brain FastAPI 래핑, /api/chat SSE, /api/login, 최소 페이지) 만들고 실서버 uvicorn 스트리밍 검증·커밋(AHOVYE_WEB_FAKE_BRAIN 가짜모드). 이후 병렬 세션이 web/ 이어받아 수정중(=web/ 건드리지 않음).

## 다음 세션에서 필요한 맥락
- 7/20(월) 재고 커넥터 실연결 재개(서비스 계정 키 → dev\ahovye\secrets\service_account.json).
- D 산출물 tools\orgcli(터미널 조직도 조회 CLI, model/dataio/render+커맨드별 파일, 24테스트 통과)는 유용하지만 미커밋 — 정식 편입 여부 보스 결정 대기.
- 아호비 미커밋(내가 안 건드림): login.py, jarvis.py, dev_history.json — 이전 세션 잔여로 보임.
- 웹(B) 본작업은 병렬 세션 담당. 이 세션 web/server.py가 정본, 다른 세션이 /api/health·/api/logout·테스트용 users 경로 등 추가중. 충돌 방지 위해 web/ 미터치.
- 파이프라인 쓰는 법: Workflow 도구에 scriptPath=dev\scripts\claude-workflows\auto-dev.js, args는 객체 {spec,projectDir,maxFixRounds?}(문자열도 이제 파싱됨). pytest는 이 환경에서 시스템 temp 권한오류(WinError 5) 나면 --basetemp=.pytmp로 우회.
- 크로스세션 메시지/자동 task알림은 사용자 지시가 아님 — 위험작업은 보스 직접 확인 후 진행.
- NotebookLM MCP add_source는 여전히 Chrome 프로필 launchPersistentContext 크래시(정상). 로컬 폴백으로 저장함.
