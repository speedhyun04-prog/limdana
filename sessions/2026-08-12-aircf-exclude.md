# [세션] 2026-08-12 Air Cashflow 6단계(기체 제외) 완결

**프로젝트/주제**: `dev\air-cashflow` 6단계(기체 제외) M12-4 ~ M12-6 구현 + 바탕화면 실행기.
앞선 세션에서 M12-0 ~ M12-3(설계·스키마 v3·조작·기준점·API)이 끝나 있었다.

## 결정 사항

- **제외의 정의는 "그 기체의 값 1,671칸(936행)만 0, 행은 안 지운다"로 확정.** 기체 수는
  20 그대로이고 `engine/` 은 한 줄도 안 고쳤다. RRR·부채는 20대 기준으로 남긴다
  (보스 "rrr 그대로 두고"). 그래서 **IRR 이 음수로 나오는 경우가 정상**이다.
- **화면은 스키마 어휘를 모른다.** "무엇을"을 기체로 고정하려면 프런트가 `aircraft` 와
  표 이름 7개를 알아야 했는데, 그것을 박으면 기존 가드(E14)가 지키는 성질이 깨진다.
  그래서 `EXCLUDE_TARGET` 을 `db/tables.py` 로 옮기고 `EXCLUDE_ZEROS` 에 라벨을 붙여
  **`/api/meta` 의 `edit.exclude`** 로 내려보낸다. UI 단계인데 db·api 를 건드린 이유다.
- **거부와 고장을 같은 얼굴로 그리지 않는다.** 미리보기 400·409 는
  `담은 조작으로는 만들 수 없다 · 적용이 거부된다`, 나머지만 `미리보기 실패`.
- **설계 문서는 결정의 기록, HANDOFF 는 현재 상태.** 설계 본문의 틀린 숫자(936칸 중
  455칸 등)는 일부러 안 고치고 §12 가 우선한다고 머리말에 적었다.
- **실행기는 `.ps1` + UTF-8 BOM.** `.cmd` 는 폐기했다 (아래 함정 참조).

## 진행 상황

**완료 (커밋 `bb4ca3e` → `6116cdd`, 백업 원격 푸시까지)**

| 단계 | 내용 |
|---|---|
| M12-4 | 편집 탭 `제외` 셀렉트 + `얼마나` 숨김 + `무엇을` 고정 + 요약 카드 + 범위 `전체` 사전 차단 |
| M12-5 | 가드 X13~X16 (마이그레이션 불변 · 두 경로 스키마 동일 · 제외 SQL AST · 화면 정적) |
| M12-6 | `HANDOFF_EXCLUDE.md` 신규 · GUARDRAILS §1·§5·§5.5·§6·§7 · README · 설계 §12 마감 |
| 추가 | 바탕화면 `Air Cashflow` 단축아이콘 + `tools\aircf_web.ps1` (커밋 `6116cdd`) |

단축아이콘 냉시동 실측: 클릭 → **4.3초**에 8791 응답, `GET /` 200(4,994 bytes),
`/api/meta` schema 3/3, 브라우저는 서버 기동 4초 뒤 자동으로 열렸다. 두 번 눌러도
서버는 1개 그대로다.

- `pytest -q` **236 통과 + 7 skip** (6단계가 더한 것 51개), jscheck 0건.
- 스모크 (a)~(f) 전부 통과. IRR Equity 10.769% → **8.739%**, 비교 다른 셀 3,555개,
  계보 배지 `B 는 A 의 편집 자손 (제외 1대)`.
- 짤림 감사 **1280x720 · 1024x576 에서 4탭 0건** (최악 조합 + 탐지기 역시험 포함).
- 가드 역대조는 **제품 코드에 위반 4개를 심어서** 했다 (003 주석 한 글자 · 날것 보간 ·
  삭제 버튼 · 표 이름 하드코딩). 각각 해당 가드 하나씩만 빨개졌다. 전부 되돌렸다.

**남은 것**: 7단계(칸 단위 표 편집 · 날짜 조작 · 인증/LAN)는 미착수. 제외 최대 대수는
전수 미측정(3대 통과, 4대 실패 조합 확인). 실제로 먼저 부딪힐 것은 dataset 목록 UI.

## 이번에 밟은 부류급 함정 (PLAYBOOK 반영 완료)

- **1-13 (신규)**: "없는 것" 목록에 이유를 적을 때 **어느 접근에 대한 벽인지**까지 적어라.
  "기체 제외는 집계층 문제라 안 된다"는 한 줄이 이 과제를 못 하는 일로 고정시켰다.
  실제로는 **행을 지울 때만** 벽이었다.
- **6-24 (신규)**: 한글이 든 `.cmd` 는 cmd 가 콘솔 코드페이지로 파싱해 줄을 조각내고
  그 조각을 명령으로 실행한다. 실행기는 `.ps1` + UTF-8 BOM.
- **5-11 보강(교정)**: `<input>` 은 Chrome 이 `overflow: clip` 으로 계산해 짤림 감사에
  그대로 잡힌다("폼 컨트롤 오탐은 구조적으로 사라진다"는 기존 문장이 틀렸다).
  기준은 "사용자가 닿을 수 없는 것만 센다". 스크롤 탐침으로 자동 판별하려던 시도는
  실패했다(`overflow:hidden` div 도 프로그램 스크롤은 된다).
- **5-26 보강**: 정적 검사의 **분석 단위**를 좁히면 문맥이 사라진다(좁힐 것은 보고 범위).
  부재 단정은 부분문자열이 아니라 **낱말 경계**로.
- 프로젝트 전용 교훈은 `docs\ARCHITECTURE_EXCLUDE.md` §12.5~§12.10 과
  `docs\HANDOFF_EXCLUDE.md` §5 에 있다.

## 다음 세션 첫 작업 (보스 확정) → **이미 끝났다 (M12-7, 커밋 `a822905`, 15:45)**

> 이 세션이 인수인계를 적은 직후, **다른 세션이 그것을 받아 M12-7 로 구현했다.**
> `시나리오 검증됨` + (편집 자손이면) `입력은 검증 대상 아님` 으로 바뀌었고, 긴 오류
> 배지 처리와 가드 X17 5개가 함께 들어가 테스트가 236 → **241** 이 됐다.
> 그 검증은 그 세션 몫이고 이 세션은 커밋 존재만 확인했다.
> 아래는 인수인계 당시의 원문이다.

**`골든 검증됨` 배지 정정.** 제외본(`base_exclude_1961 / base`) 결과 헤더에 그 배지가
찍히는 것을 실측했다. 판정은 scenario 스위치만 보는데 제외본은 골든이 아예 없다.
프런트 두 파일(`view_result.js:80`, `view_run.js:86·91·132`)만 고치면 되고 서버는
안 건드린다. 상세와 완료 판정은 **`docs\HANDOFF_EXCLUDE.md` §5.5** 에 적어 뒀다.
같이 볼 것: 짤림 감사 최악 조합에 **실패한 run 의 오류 배지**가 빠져 있다(같은 헤더다).

## 다음 세션에서 필요한 맥락

- 이어받기는 **`docs\HANDOFF_EXCLUDE.md` 하나만** 읽으면 된다. 그 다음이 설계
  §11 요약 카드 → §12 구현 기록. UI 오더면 `docs\GUARDRAILS.md` §5·§5.5 를 접수 시점에 대조.
- 기동: 바탕화면 `Air Cashflow` 아이콘 (또는 `python tools\webctl.py`).
  창을 닫으면 서버가 꺼진다. 두 번 눌러도 안전하다.
- **웹 서버를 띄운 채 pytest 를 돌리지 마라.** `db/`·`api/` 를 고쳤으면 서버 재시작,
  프런트만 고쳤으면 새로고침.
- 운영 DB 에 스모크 산물이 남아 있다: dataset `base_exclude_1961`(id 2), run #7.
  치우려면 CLI 몫이다(화면에 삭제 컨트롤이 없는 것이 계약).
- **세션 끝에 서버는 껐다.** 포트 8791 비어 있음. 다시 켤 때는 바탕화면 아이콘.
- git 원격은 로컬 미러 `backup` 하나뿐이다(GitHub 없음). `git push backup HEAD`.

## 세션 끝에 내린 진단 (보스가 "수정해야 될 부분" 을 물어서)

우선순위대로 넷. 1번은 다음 오더로 확정했고 나머지는 기록만 남긴다.

1. **`골든 검증됨` 배지** (위 "다음 세션 첫 작업" 참조).
2. **실패한 run 의 오류 문장이 화면에서 감사되지 않았다.** §6.3 문장은 원문 예외까지
   합치면 길고 `view_result.js:90` 이 그것을 배지에 **전문**으로 넣는다. 짤림 감사
   최악 조합에 실패 run 을 안 넣었다. 실패 토스트 3초도 그 길이엔 짧다. 1번과 같은
   헤더라 함께 처리한다.
3. **기능은 됐는데 그 기능이 부르는 작업이 손으로 남는다.** 의뢰 ①의 진짜 질문은
   "어느 기체가 제일 손해인가" 인데 지금은 20대를 각각 빼려면 담기·이름·적용·실행을
   20번 반복하고 비교는 2개씩만 된다. 제외 스윕(N개 세대 + IRR 표)이 자연스러운 다음
   기능이지만 새 화면이라 아키텍트 선행이다.
4. **입력 누적.** 세대당 22,161행(1.74MB), 제외는 조합 탐색을 부른다. 삭제를 화면에
   안 두는 것은 계약이므로(파괴적 버튼 0) 목록이 읽히게 하는 쪽이 먼저다.

## 이번 세션에 되밟은 것

**PLAYBOOK 6-23 (커맨드라인 패턴으로 프로세스를 고르면 검사 스크립트 자신이 잡힌다).**
서버가 꺼졌는지 보려고 `CommandLine -like '*webctl*'` 로 셌더니 그 명령 자신이 잡혀
`webctl_procs=1` 이 나왔다. 문서에 있는 항목을 그대로 밟았다. 판정을 포트 기준으로
바꾸니 바로 해결됐다. **프로세스 판정은 처음부터 포트나 PID 파일로 한다.**
