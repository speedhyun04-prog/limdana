# [세션] 2026-08-12 멈춘 작업 전수 정리 · 배포 신원 사고 · M3 · 그룹웨어 일정

**날짜**: 2026-08-11 시작 → 2026-08-12 이어짐
**프로젝트/주제**: 아호비. "진행되다 멈춘 세션" 전수 조사에서 시작해, 배포 정체
사고 2건을 잡고, 메뉴바 M3 와 그룹웨어 §9.2 백엔드까지.

## 결정 사항

- **커밋·푸시는 작업 단위마다 자율.** 보스 지시(8/11). 릴리스(사원 66명 배포)는
  여전히 승인 대상. 메모리 `git-push-autonomy`.
- **통화(보이스콜·페이스톡)는 HTTPS 선행.** getUserMedia 는 secure context 에서만
  존재하고 루프백만 예외라, 지금 켜면 콤바인에서만 되고 사원 브라우저에서는 API
  자체가 없는 반쪽 기능이 된다 → **서버 이전 완료 후 착수**. 버튼은 hidden 유지.
- **진단 로그는 지우지 않고 스위치로.** 지우려던 `[shell-diag]` 가 당일 버그 2건을
  잡아냈다. 개발기 켬 · 배포판 끔 · `AHOVYE_DIAG=1` 강제.
- **자원 예약은 전사(company) 일정 전용**, 경계 접촉(9~10시 / 10~11시)은 겹침 아님.
- **릴리스는 모아서 굽는다.** 하나씩 굽는 건 66명 × 151MB 낭비.

## 진행 상황

### 완료 (커밋 8개, 전부 푸시됨)

| 커밋 | 내용 |
|---|---|
| `cf0065a` | 모션 표시 전환 소유권 정리(gw 셀렉트 계약 편입) |
| `b15c6d2` | **브리지 CORS 허용목록 반사** (실측 16/16) |
| `88e7e80` | 진단 로그 스위치화 + **DevTools 포트 배포 노출 차단(보안)** |
| `33572ef` | 통화 설계 `ARCHITECTURE_CALLS.md` 신규 |
| `ced481d` | **M3 ← → 화면 히스토리** `nav.js` (스모크 22) |
| `48808fb` | **도움말 > 업데이트 확인** 브리지 창구 (스모크 23) |
| `c62926c` | 휴가·연차 스모크 24 (이미 구현된 기능의 회귀 가드) |
| `16c50cf` | **일정·자원예약 백엔드** §9.2 (스모크 24) |

**0.7.8 릴리스·설치 완료.** 실기 확인: 셸 빌드 줄, 창 분리(로그), Alt+Space 오브
소환, ← 히스토리 전부 정상.

### 발견한 사고 3건

1. **설치판이 0.7.5 에 9일 정체.** 증거 3중 일치(exe 타임스탬프 · 실행 중 브리지
   `version 1.1.0` + owner 필드 없음 · BUILD.json). 0.7.6 단축키 교착 수정과 0.7.7
   배포 신원이 이 PC 에 없었다. 원인은 트레이 "지금 업데이트"가 **이미 받아둔
   설치판이 있을 때만 활성**이라(`pending_update() is not None`) 확인을 시작할
   방법 자체가 없었던 것 → 그래서 M3 업데이트 창구를 만들었다.
2. **브리지 CORS 고정 발행.** `server_url`(`.local` 이름) 하나만 허용해서 콤바인
   웹뷰(`127.0.0.1:8765`)의 `/api/session`·`/api/status` 가 전부 preflight 차단.
   오브 입력창 토큰 공유와 로그인 PIN 경로가 **조용히** 죽어 있었다.
3. **DevTools 포트(9223)가 배포판에도 열려 있었다.** `studio_web.pyw` 가 배포
   대상(`bundle_payload` PAYLOAD_FILES)인 걸 놓친 자리. 사내 66명 배포 전에 차단.

### 반대 방향 발견 2건 (이미 되어 있던 것)

- `requirements.txt` 부채 → 8/5 `deploy/requirements-server.txt` 로 해소돼 있었다.
- **그룹웨어 휴가·연차 → 7/29 에 백엔드·프런트 전부 구현 완료.** 다시 만들 뻔했다.
  새로 만드는 대신 스모크를 붙였다(그룹웨어는 스모크가 0 이었다).
  → 이 부류를 PLAYBOOK **5-29** 로 승격.

### 남은 것

- **그룹웨어 📅 탭 프런트** (다음 세션 1순위). API 는 준비 완료.
- M3 나머지(문제 보고 · 화면 캡처), P3 B2C 가드, 설치판 브랜드 아이콘 누락(#17)
- 보스 몫 육안 4건: AI 세션 배지 · G8 인원칩 · 이모티콘 짤림 · 작업 패널
- AWS 계정(서버 이전 2단계), studio.py 데드라인, 맥 빌드, 살롱허브 8/6 미팅 후속

## 다음 세션에서 필요한 맥락

- **착수 첫 행동은 코드로 실재 확인**(PLAYBOOK 5-29). 이번 세션에 2번 밟았다.
- **미배포 커밋 4개**(CORS · M3 · 업데이트 창구 · 일정 백엔드). 다음 릴리스 = 0.7.9.
  업데이트 확인 메뉴는 그때부터 실동작(지금 도는 브리지는 1.2.1).
- **실DB `web/groupware.db` 는 아직 user_version=1.** 서버 재시작 시 v2 마이그레이션이
  돈다. 사본으로 무손실 검증 완료(근태 4·연차 6·공지 8 행수 무변화).
- 📅 탭 설계: `docs/ARCHITECTURE_GROUPWARE.md` §9.2. 월 그리드 순수 JS(라이브러리
  금지), 자원예약은 등록 시트의 셀렉트 하나로 흡수(별도 탭 금지).
- 커밋 메시지에 큰따옴표가 있으면 PowerShell here-string 이 깨진다 →
  스크래치패드 파일 + `git commit -F`.
- 스모크가 26종으로 늘었고 pre-push 훅이 전부 돌린다(신규: bridge · nav ·
  gw_leave · gw_events).

## PLAYBOOK 갱신

- **2-13** CORS 허용 오리진 고정 발행 금지 → 요청 Origin 반사 + 허용목록 + Vary.
- **5-29** "남은 일" 목록은 낡는다. 착수 전 코드로 실재 확인, 이미 있으면 만들지
  말고 검증을 붙인다.
