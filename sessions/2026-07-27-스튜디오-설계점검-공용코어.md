# [세션] 2026-07-27 아호비 스튜디오 설계 점검 + 공용 코어 추출

- **날짜**: 2026-07-27
- **프로젝트/주제**: 아호비 스튜디오(데스크톱 studio.py + 웹 web/server.py) 유지보수성 설계 점검, 승인받은 1·2번 수정 실행

## 결정 사항
- 설계 점검 결과 5가지 문제를 보고, 보스가 그중 1·2번만 승인:
  1) rooms(사내 대화방) 동시성 수정 (실행함)
  2) 데스크톱·웹 중복 코드 공용 모듈 추출 (실행함)
  3) rooms를 web/rooms_api.py 라우터로 분리 (보류)
  4) sessions.list_sessions 메타 캐시 + 검색 디바운스 (보류, 세션 수백 개 되면 성능 문제)
  5) studio.py 2,700줄 분리 (보류 권장, 웹 전환 방향이라 손익 안 맞음)
- 새 도구/엔진/언어 규칙은 앞으로 chat_common.py에만 추가한다 (studio.py, server.py 양쪽 복사 금지).

## 진행 상황
- **신규 `dev\ahovye\chat_common.py`**: tool_event(도구→타임라인/아티팩트), lang_from_path(확장자→언어 22종 합집합), ENGINES/resolve_model(엔진 레지스트리, brain 지연 임포트). `__main__` 자체검사 포함.
- **studio.py**: _on_tool 27줄→6줄, ENGINE_DESC/ENGINE_ITEMS/_force_model/_EXT_LANG 전부 chat_common 참조로 교체. 부수효과: PowerShell 도구가 "⌨ 실행"으로 통일(이전 "🔧").
- **web/server.py**: _tool_events/_LANGS/_lang_from_path 삭제→chat_common, _ENGINES는 chat_common에서 파생(GPT 2종은 server 전용 유지). _ROOMS_LOCK을 RLock으로 바꾸고 rooms_open/rooms_send의 읽기→수정→저장 전체를 잠금(동시 send 유실 방지, 구조적 보장).
- **검증**: pycheck 3파일 OK. 격리 스모크(포트 8799, AHOVYE_WEB_* env로 실데이터 격리, 가짜 두뇌) 8항목 전부 통과. 특히 테스트 계정 2개 스레드 20개 동시 send → 20/20 보존. studio.py 오프스크린 렌더 스모크도 통과.
- **실서버 재시작 완료**: 8765 pythonw 내리고 run_server.pyw로 재기동. health OK, 로그인 세션 13개 state.json에서 복원(재로그인 불필요), 엔진 7종 정상.

## 다음 세션에서 필요한 맥락
- server.py는 이 세션 도중 다른 세션이 이미지 업로드 API(/api/upload_image, /files/{name}, rooms 이미지 메시지)를 추가해 줄번호가 밀렸었음. 충돌 없이 병합됨.
- 보류 항목 3·4·5는 위 결정 사항 참조. 4번(세션 목록 O(N) 전체 파싱)이 다음 성능 시한폭탄.
- 점검에서 인지만 한 낮은 우선순위: brain.py의 Popen 몽키패치와 SDK 내부(_ACTIVE_CHILDREN) 접근은 claude-agent-sdk 버전업 시 확인 필요. LEGACY_OWNER="임다나" 하드코딩 다수. 데이터 파일(sessions/, rooms.json 등)이 코드 폴더에 혼재. web/static/app.js 72KB 단일 파일.
- 스모크 재현: ahovye 폴더에서 AHOVYE_WEB_FAKE_BRAIN=1 + AHOVYE_WEB_USERS/STATE/ROOMS/CONVO_DIR/PROJECTS_DIR 격리 env로 uvicorn 별도 포트 기동. 서버 재시작은 8765 pythonw 종료 후 `Start-Process -WindowStyle Hidden pythonw web\run_server.pyw`.
