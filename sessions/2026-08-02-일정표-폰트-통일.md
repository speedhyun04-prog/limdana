# [세션] 2026-08-02 아호비 일정표 폰트 통일 (자체 날짜·시간 선택기)

- **날짜**: 2026-08-02
- **프로젝트/주제**: 아호비 웹 스튜디오, + 메뉴 일정·할 일 모달의 시각 입력 폰트 통일

## 결정 사항
- 네이티브 datetime-local 팝업(달력·시간 목록)은 브라우저가 OS 레벨로 그려서 앱 폰트(Pretendard)를 못 입힌다는 결론. CSS로는 불가, 자체 위젯으로 교체.
- 자체 선택기는 팝오버가 아니라 모달 안 인라인 펼침으로 (z-index·짤림 위험 제거).
- 분 단위는 5분 스텝, 기본값은 지금+10분 올림 (plus_schedule.js 선례 따름).
- 선택기 열림 동안 일정 목록 max-height를 90px로 줄여 낮은 화면(배율 125%)에서 모달 세로 넘침 방지.

## 진행 상황 (완료)
- `web/static/plus_organizer.js`: #plCalAt을 readonly 텍스트 입력으로 교체, 자체 선택기(pl-dtp) 구현. 값 계약은 `atEl.dataset.val = "YYYY-MM-DDTHH:MM"` 하나, 표시값은 한글("2026년 8월 15일 (토) 오전 9:30"). 달력 그리드(월 이동, 오늘 버튼, is-today/is-on), 오전/오후 토글, 시(01-12)·분(5분 스텝) 스크롤 컬럼.
- `web/static/app.css`: .pl-dtp* 스타일 추가. 라벤더 토큰(var(--accent) 계열)만 사용해 시큐어 모드(초록) 자동 대응, 폰트는 본문 상속.
- `web/index.html`: ?v= 갱신 (이후 다른 세션이 app.css를 20260802g로 재갱신).
- 검증: 실제 화면에서 Pretendard 적용, UI_GUARDRAILS §5.5 짤림 감사 기본폭·420×760 모두 0건, 날짜/시간 선택→추가→저장 동작 확인, 테스트 데이터 원복.
- 커밋/푸시: 다른 세션의 일괄 커밋 4b4a17f(feat persona + 오늘 작업 일괄)에 포함되어 이미 origin/main에 푸시 완료(3f93914 기준 동기화). 별도 커밋 불필요로 확정.
- PLAYBOOK 6-17 추가: 브라우저 네이티브 폼 위젯 팝업(date/time/select 목록)은 앱 폰트·테마를 못 입힌다 → 폰트 통일 표면은 자체 위젯, 인라인 펼침 권장.

## 다음 세션에서 필요한 맥락
- 잔여 네이티브 입력 2곳도 같은 문제: `gw.js` 259·261줄 `<input type="date">`(전자결재 휴가 기간 gwLs/gwLe), `settings.js` 114-115줄 `<input type="time">`(방해금지 stDndFrom/stDndTo). 작업 칩(task_8fd4cd52)으로 걸어둠.
- 교체 시 plus_organizer.js의 pl-dtp 패턴 재사용(날짜만/시간만 축소판). 완료 기준: §5.5 감사 0, index.html ?v= 갱신, 시큐어(html.secure) 토큰 확인.
- 관련 파일: `C:\Users\USER\dev\ahovye\web\static\plus_organizer.js`, `app.css`(.pl-dtp 블록), `docs/UI_GUARDRAILS.md` §5.5.
