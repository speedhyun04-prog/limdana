# [세션] 2026-07-19 세션 병렬화와 웹 전환 B트랙 완료

- **날짜**: 2026-07-19
- **프로젝트/주제**: 세션 병렬 오케스트레이션 + 아호비 웹 전환(트랙 B) 완료

## 결정 사항
- 오늘 계획 4트랙(A 조직도 마무리 / B 웹 전환 / C 디자인팀 스튜디오 통합 / D 파이프라인 실전 검증)을 한 세션 순차가 아니라 병렬로 재편.
- C는 studio.py 충돌 위험 때문에 기존 세션("멀티 에이전트 아키텍처 설계")에 유지, D는 그 세션의 백그라운드 워크플로, B는 별도 병렬 실행(이 세션의 백그라운드 에이전트)으로 분리.
- web/ 폴더 소유권은 B 세션(이쪽)으로 이관, 기존 세션은 web/에서 손 뗌.

## 진행 상황 (전 트랙 완료)
- **B 완료**: `dev\ahovye\web\server.py` 확장(FastAPI). 엔드포인트: `GET /api/health`, `POST /api/login`, `POST /api/logout`, `POST /api/chat`(SSE 스트리밍), 127.0.0.1:8765. `web\index.html` 신규(로그인+대화창 시제품, CSS 디자인 토큰 기반, sessionStorage 토큰, SSE 렌더). 스모크 전부 통과(가짜 두뇌 모드 `AHOVYE_WEB_FAKE_BRAIN=1` + 임시 users.json, 실데이터/실호출 미사용, 테스트 후 서버 종료·포트 해제).
- **C 완료**(기존 세션): 디자인팀(🎨) 토글 스튜디오 통합, 개발진(👥)과 상호배타, admin 전용 표시.
- **D 완료**(기존 세션): auto-dev 파이프라인 실전 검증 성공. 에이전트 12개가 조직도 CLI(`dev\ahovye\tools\orgcli`, 서브커맨드 search/dept/contact/tree) 생성, 테스트 24개 통과, 실데이터 엣지케이스(tree 루트 2개, 후행 공백 부서명) 자동 처리.
- A는 이 세션 시작 전에 이미 완료(조직도 MCP 스모크 + WorkOS 메일 이름→이메일 해석 계층).

## 오후 추가 진행 (같은 세션, 랩업 이후)
- **병렬 2트랙 추가 완료**: (1) 웹 고도화 = 레퍼런스 디자인 이식(페이퍼+바이올렛 #6657FF, 9._.o 워드마크, 위안 레이어) + 토큰 TTL(12h)·state.json 영속화(토큰 SHA-256 해시만) + 실제 brain 스모크 1회 성공. (2) 디자인팀 검증 = 정적검증·오프스크린 스모크 27/27·실전 파이프라인 1회 완주(날짜 배지 각짐 보정, 기준서 §6 규칙 자율 축적). 버그픽스: 시큐어 모드에서 design_btn 잠금 누락.
- **웹 서버 상주화**: 셸 종료 시 서버가 죽던 문제 해결. `web\run_server.pyw`(pythonw 무창, 크래시 자동 재시작, 중복 방지, 로그 web\server.log) + 시작프로그램 등록(`ahovye_web.vbs`, 지우면 해제). 현재 서버는 셸과 분리된 프로세스로 실행 중(127.0.0.1:8765, 실제 두뇌 모드, 개인 모드 익명 접속).
- **커밋 정리 6건**: 디자인팀 검증(4103d7c), 웹 고도화(9fbd532), orgcli(f0c77c4), 오브·로그인 애니메이션(96a0f8b, 07-18 작업분), dev_history 장부(a395cd8), .pytmp 정리+gitignore(28c85fe). 진행 맵·웹 초안·커넥터는 다른 세션이 커밋(b00f9b0 등).
- 미커밋 잔여: dev_history.json(커밋 훅이 자동 기록, 방치 OK), docs\소개서_젠스파크_프롬프트.md(출처 불명 새 파일, 보스 확인 필요).

## 저녁 추가 진행 (온보딩 + 콤바인, 같은 세션)
- **온보딩 3종 완성·커밋** (fd4c76f~13f21ba): BA 발급 시스템(/admin, 이카운트 방식, 감사 로그), 자동시작(HKCU Run, launch_target 추상화), PIN(DPAPI+PBKDF2 기기 봉인, 5회 실패 삭제). 설계 docs\ARCHITECTURE_ONBOARDING.md, 모듈 M1~M5, 통합 검증 10/10.
- **콤바인 완성·커밋** (5bee99f~454830d): studio_web.pyw + combine\ 웹뷰 앱(GPU 소프트웨어 렌더링, 단일 인스턴스 가드 8767, 브리지 8766 선점자 담당, 서버 존속). 기존 studio.py 무변경. 보스 검수 후 **앱형 전환 완료**: %LOCALAPPDATA%\Ahovye\client_config.json 에 launch_target=app (저장소 기본은 web형 유지).
- **보스 계정 생성**: uid=boss, admin+accounts.manage, 임시 비번은 첫 로그인 강제 변경 예정 상태로 전달함. auth.py add CLI는 getpass 대화형이라 비대화 셸에서 멈춤(주의), 함수 호출로 우회.
- **캐시 이슈 수정** (214f54f): GET / 에 Cache-Control no-store. "로그인 카드 없음" 신고의 실체는 버그 아님, 화면이 작업 화면으로 시작하고 로그인 카드는 상단 바 "로그인" 버튼으로 여는 구조 (다른 세션의 대규모 UI 개편 결과). 자동화 진단 시 visibility 트랜지션(.6s)이 백그라운드 패널에서 멈춰 hidden으로 보이는 함정 있음(transition 제거하면 즉시 visible, 실사용 무관).
- 커밋 안 한 파일: docs\소개서_젠스파크_프롬프트.md (보스 파일, 출처 확인 필요).

## 다음 세션 과제 (보스 예고: 대대적 수정)
- 웹 UI 대대적 수정 예정. 제안해 둔 것: 계정 모드에서 첫 화면부터 로그인 카드 자동 표시 (§0 대전제).
- 이후 로드맵: 베타 4명 배포 (사내 서버 위치 + HTTPS/바인딩 결정 필요), 시큐어 모드의 웹 재정의, 트레이 에이전트 실전 육안 확인(client\agent.pyw 머리말 절차).
- 재고 커넥터 서비스 계정 키 재개 2026-07-20(월).

## 다음 세션에서 필요한 맥락
- 웹 서버 실행: ahovye 폴더에서 `python -m uvicorn web.server:app --host 127.0.0.1 --port 8765`
- B 후속 과제:
  1. 실제 brain 연동 스모크 (현재 가짜 모드만 검증)
  2. 토큰 TTL·세션 영속화 (현재 인메모리)
  3. LAN 공개 시 HTTPS/바인딩 정책 (현재 localhost 전용)
  4. 레퍼런스 디자인 확정 후 index.html 토큰 교체 + 스튜디오 기능(모델 선택 등) 이식
- 재고 커넥터 서비스 계정 키 재개는 2026-07-20(월).
- 병렬 진행 시 규칙: 같은 파일을 두 세션이 수정하지 않게 폴더/파일 단위로 소유권을 나눌 것.
